@model HoangManhTungBTH_02.Models.Student

@{
    ViewData["Title"] = "Edit";
}

<h4>Sửa thông tin sinh viên</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="StudentID" />
            <div class="form-group">
                <labael asp-for="StudentName" class="control-label"></labael>
                <input asp-for="StudentName" class="form-control" />
                <span asp-vaidation-for="StudentName" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
                <a class="btn btn-success" asp-action="Index">Danh sách sinh viên</a>
            </div>
        </form>
    </div>
</div>

@section Sripts {
    @{await Html.RenderPartialAsync("_validationScriptsPartial");}
}